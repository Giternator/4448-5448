package com.example.healthmetrics;

public class bloodPressure extends Metric{
	public int check(int age)
	{
		return 0;
		
		//TODO 
		//get the latest value from db
		//value = 
		
		/*
		//get the latest value stored in database
		int value = 0;
		if(age >= 15 && age <= 19)
				if(value >= 105 && value <= 120)
					return 1;
				else
					return 0;
		if(age >= 15 && age <= 19)
			if(value >= 105 && value <= 120)
				return 1;
			else
				return 0;
				
		*/
		
		
	}
}
